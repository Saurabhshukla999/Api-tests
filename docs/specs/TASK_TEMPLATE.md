# Task: <short outcome>

Copy to `docs/specs/<task-name>.md` for substantial work. Replace the prompts with
facts; do not put tokens, customer data or raw HTTP payloads in this document.

## Problem and scope

- Observable problem and expected behavior:
- Case/defect IDs and source evidence:
- Included changes:
- Out of scope:

## Execution context

- Branch and starting revision:
- Affected classes/helpers and callers:
- Local-only or live validation:
- For live validation: authorized environment, tenant roles (no tokens), run
  ownership, appointment budget and intended test selector:
- Data created/changed and settings to restore:

## Acceptance criteria

List observable conditions. For a journey include relevant response fields,
independently calculated expected money, report keys/deltas and failure cases.
For a bug specify the smallest check that would fail before the fix.

## Implementation and commits

Describe the smallest change using existing helpers, then list logical commits.
Record material deviations and their evidence as work progresses.

## Validation evidence

| Command/check | Result | What it establishes / limitation |
|---|---|---|
| Compile-only command | Pending | Test sources compile; no API behavior verified |
| Relevant regression or live selector | Pending / not applicable | State reason and actual counts when run |
| Diff and credential review | Pending | Review only intended changes and safe artifacts |

## Handoff

Record completed criteria, unresolved issues, checks not run and why, settings
restored, residual test data, and any next action needed from the environment owner.
